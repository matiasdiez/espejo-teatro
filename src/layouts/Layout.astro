---
import '@styles/global.css';
import Navbar from '@components/Navbar.astro';
import Head from '@components/Head.astro';

// Definimos las propiedades que las páginas podrán pasarle al Layout
interface Props {
	title: string;
	description?: string;
	image?: string;
	showPrintButton?: boolean;
}

const { title, description, image, showPrintButton = false } = Astro.props;
---
<!doctype html>
<html lang="es" data-theme="dark">
	<head>
		<Head title={title} description={description} image={image} />
	</head>
	<body class="bg-calcareo-50 text-calcareo-900 dark:bg-calcareo-950 dark:text-calcareo-50 min-h-screen flex flex-col overflow-x-hidden transition-colors duration-300">
		<!-- Insertamos el Navbar globalmente -->
		<Navbar showPrintButton={showPrintButton} />
		
		<!-- Aquí se inyectará el contenido específico de cada página -->
		<slot />

		<script>
			// Importamos las dependencias instaladas vía pnpm
			import "flyonui/flyonui";
			import { createIcons, icons } from 'lucide';

			document.addEventListener('DOMContentLoaded', () => {
				// Inicializar íconos
				createIcons({ icons });

				// Lógica de Theme Toggle
				const themeToggle = document.getElementById('theme-toggle');
				const htmlTag = document.documentElement;

				const savedTheme = localStorage.getItem('theme');
				if (savedTheme) {
					htmlTag.setAttribute('data-theme', savedTheme);
					if(themeToggle) themeToggle.checked = savedTheme === 'dark';
				} else {
					const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
					htmlTag.setAttribute('data-theme', prefersDark ? 'dark' : 'light');
					if(themeToggle) themeToggle.checked = prefersDark;
				}

				if(themeToggle) {
					themeToggle.addEventListener('change', (e) => {
						const newTheme = e.target.checked ? 'dark' : 'light';
						htmlTag.setAttribute('data-theme', newTheme);
						localStorage.setItem('theme', newTheme);
					});
				}
			});
		</script>
	</body>
</html>
